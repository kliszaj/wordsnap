services:
  wordsnap:
    build:
      context: ./backend
    container_name: wordsnap
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/data:/app/data

