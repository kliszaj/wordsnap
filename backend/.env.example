OPENAI_API_KEY=
ANTHROPIC_API_KEY=

WHISPER_MODEL=whisper-1
CLAUDE_MODEL=claude-sonnet-5

# Optional, only needed when running compare.py for A/B checks.
GPT_MODEL=gpt-5.5

# Optional. If set, Save to Anki sends directly through AnkiConnect.
# Leave blank to keep using CSV fallback.
ANKI_CONNECT_URL=http://anki:8765
ANKI_DECK=Swedish::WordSnap
